# Docker Environment Configuration
# Copy this file to .env.prod and fill in the values for production deployment

# Database Configuration
DB_USER=postgres
DB_PASSWORD=change-this-in-production
DB_NAME=whois_it

# Backend Configuration
JWT_SECRET=change-this-to-a-very-secure-random-key-in-production
JWT_EXPIRATION=7d
PORT=4000

# Frontend URLs (for production)
FRONTEND_ORIGIN=https://yourdomain.com
FRONTEND_URL=https://yourdomain.com

# API URLs (accessible from browser)
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
NEXT_PUBLIC_SOCKET_URL=https://api.yourdomain.com

# Email Configuration (Optional - emails will be logged to console if not configured)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=noreply@whoisit.com
